<!--Hooks : React Hooks are functions that allow functional components in React to manage state, handle side effects, and access other React features without needing class components. They provide a simpler and more efficient way to manage component logic. -->

<!--React Hook Form :  https://react-hook-form.com/get-started -->


<!-- Uncontrolled Component kya hota hai?
Form input ka data browser DOM ke paas hota hai, React ke state ke andar nahi.

Example: Hum input mein value aur onChange nahi likhte.

Hum input ke value ko ref ke through access karte hain.

📌 Controlled Component kya hota hai?
Form input ka data React state ke andar hota hai.

Example: value={inputValue} aur onChange={e => setInputValue(e.target.value)} use karte hain -->